export const selectCurrentAddress = state => state.wallet.currentAddress;
export const selectCurrentBalance = state => state.wallet.currentBalance;
export const selectIsLoading = state => state.wallet.isLoading;
export const selectError = state => state.wallet.error;
